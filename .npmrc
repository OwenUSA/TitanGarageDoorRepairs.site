# Hostinger's build image runs pnpm 11.x through corepack, which cannot switch
# to the pinned pnpm@10.25.0 and hard-fails the install. Downgrade that check
# to a warning so the deploy proceeds with whatever pnpm the host provides.
pm-on-fail=warn
