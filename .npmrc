# Hostinger's build image runs pnpm through corepack. pnpm refuses to switch
# versions under corepack, so a "packageManager" pin in package.json aborts the
# install outright (the pin has been removed for that reason). This setting is a
# second line of defence if a pin or devEngines block is ever reintroduced.
pm-on-fail=warn
